import { MissionMode } from "../ui/mission/MissionMode";
import type { WorkspacePageProps } from "../app/app_routes";

export default function MissionRoute(_: WorkspacePageProps): JSX.Element {
  return <MissionMode />;
}
